{
  "name": "juni-ai",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "JUNI-AI multi-provider AI agent foundation.",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check api/chat.js && node --check api/auth.js && node --check api/research.js && node --check api/answers.js && node --check api/voice-token.js && node --check api/health.js",
    "dev": "npx serve .",
    "export:answers": "node scripts/export-approved-answers.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.127.0",
    "@google/genai": "2.23.0",
    "openai": "7.20.0",
    "@libsql/client": "0.18.0"
  }
}

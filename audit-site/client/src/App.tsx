import Home from "./pages/Home";

// Evidence Ledger: editorial brutalism for a service-layer audit. Keep the shell asymmetric,
// paper-led, and dense with small signal labels rather than generic dashboard chrome.
export default function App() {
  return <Home />;
}
